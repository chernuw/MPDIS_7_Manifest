<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chernuwnote.mpdis_7_manifest">
    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"
            android:icon="@mipmap/myicon">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".DroidActivity2"
            android:icon="@mipmap/myicon2"
            android:label="MPDIS_7_Second">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <permission android:name="android.permissions.CAMERA" />
    <permission android:name="android.permissions.WRITE_CONTACTS" />
    <permission android:name="android.permissions.READ_CONTACTS" />
    <permission android:name="android.permissions.WIFI_STATE" />
    <permission android:name="android.permissions.BATTERY_STATE" />

</manifest>